{% extends 'layout/base.html.twig' %}

{% block title %}Seccion{% endblock %}

{% block body %}
    <h1>Seccion</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ seccion.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ seccion.nombre }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ seccion.descripcion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_seccion_index') }}">back to list</a>

    <a href="{{ path('app_seccion_edit', {'id': seccion.id}) }}">edit</a>

    {{ include('seccion/_delete_form.html.twig') }}
{% endblock %}
